apiVersion: batch/v1
kind: Job
metadata:
  name: ubuntu-job
  namespace: default
spec:
  template:
    spec:
      terminationGracePeriodSeconds: 1
      containers:
      - name: ubuntu
        image: ubuntu
        command: ["bash", "-c", "while true; do sleep 5; ls; done"]
      restartPolicy: OnFailure
